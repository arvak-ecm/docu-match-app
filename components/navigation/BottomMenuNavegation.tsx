import React from "react";
import { StyleSheet } from "react-native";

const BottomMenuNavegation = () => {
  return <Tabs screenOptions={{ tabBarActiveTintColor: "blue" }}></Tabs>;
};

export default BottomMenuNavegation;

const styles = StyleSheet.create({});
